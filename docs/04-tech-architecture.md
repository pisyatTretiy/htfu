# 04. Техническая архитектура

## 4.1 Выбор стека

### Рекомендация: TypeScript + PixiJS 8 + Vite, собственная 2D-физика

| Критерий | Оценка |
|---|---|
| Вес билда | ~1 МБ движка + ассеты → реально уложиться в 15–25 МБ при лимите 100 МБ |
| Старт игры | Первый экран < 3 сек на 4G — критично для удержания на портале |
| Производительность | WebGL-батчинг PixiJS даёт 60 FPS в мобильном браузере на бюджетном телефоне при 2D-сцене |
| Контроль над SDK | Прямая интеграция без прослоек плагинов, полный контроль над `LoadingAPI`/`GameplayAPI` |
| Физика | Своя верле-леска + импульсы — 200 строк, вместо тяжёлого физдвижка |

### Рассмотренные альтернативы

| Вариант | Плюсы | Минусы | Вердикт |
|---|---|---|---|
| **Phaser 3** | Всё из коробки: сцены, ввод, тайлмапы, аудио | Больше вес, меньше контроля над рендером | Годная замена, если команда уже знает Phaser |
| **Godot 4 (web export)** | Удобный редактор, сцены, готовые плагины Яндекс SDK | Пустой web-билд ~5 МБ сжатого, типовой проект 15–25 МБ; долгий старт wasm; просадки в мобильных браузерах | Только если у команды сильная экспертиза в Godot |
| **Unity WebGL** | Зрелая экосистема, C# | Пустой 3D-билд ~10 МБ, реальные проекты 30–50 МБ; тяжёлый старт и слабая производительность в мобильных браузерах | **Не рекомендуется** для этого проекта |
| **Three.js + Rapier (3D)** | Ближе к оригиналу визуально | Управление одной рукой ломается, риск по FPS и весу, дольше в разработке | Не для MVP |

Решение зафиксировано в [ADR-0001](adr/0001-stack.md) — там же полный список библиотек с
лицензиями и трёхдневный спайк, который подтверждает или опровергает выбор до фазы 1. Смена стека
после вертикального среза недопустима.

### Физика — это подача, а не источник истины

Исход боя считает детерминированная система на данных: натяжение растёт по формуле, усталость
рыбы падает по формуле, паттерн рывков лежит в `content/fish.json`. Верле-леска, изгиб удилища и
рывки камеры — визуализация уже посчитанного результата.

Симуляция, из которой выводится исход, невоспроизводима, не поддаётся балансировке и меняет
поведение при просадке FPS — игрок проигрывает из-за железа. Плюс у нас лидерборды:
недетерминированный бой означает нечестные рекорды.

Правило для кода: `FightSystem` работает на фиксированном шаге симуляции и ничего не знает о
рендере; `LineRope` читает его состояние и никогда в него не пишет.

## 4.2 Структура репозитория

Дерево ниже — фактическое, а не план. Рядом с файлами тестов лежат сами модули: так проще держать
их в одном изменении.

```
htfu/
├── docs/                      # план, ADR, журнал разработки, чек-лист модерации
├── index.html                 # корень архива: разметка, стили интерфейса, экран загрузки
├── src/
│   ├── main.ts                # бутстрап, проверка WebGL, экран отказа
│   ├── core/
│   │   ├── App.ts             # цикл, ввод, фокус, склейка всех систем
│   │   ├── Quality.ts         # профили качества: разрешение, тени, сетка воды
│   │   ├── Rng.ts             # сидируемый ГПСЧ — воспроизводимые тесты баланса
│   │   └── world.ts           # мелкая математика
│   ├── gameplay/              # правила, ничего не знают о рендере
│   │   ├── FightSystem.ts     # натяжение против усталости, фазы, предел лески
│   │   ├── CatchPool.ts       # что клюнет: глубина, локация, доля мусора
│   │   ├── Rarity.ts          # обычный / редкий / золотой и цена варианта
│   │   ├── Mischief.ts        # улов буянит на настиле
│   │   └── Simulate.ts        # модели игрока для подгонки и тестов баланса
│   ├── meta/                  # прогресс между забросами
│   │   ├── Progression.ts     # четыре ветки снасти
│   │   ├── Album.ts           # коллекция видов и вариантов, бонусы за заполнение
│   │   ├── Quests.ts          # цепочка заданий NPC
│   │   ├── Dailies.ts         # дела на день, серия дней, сундук
│   │   ├── Zones.ts           # локации, гейты, особенности
│   │   ├── Bosses.ts          # боссы локаций и трофеи
│   │   ├── Boosts.ts          # приманка и другие временные бонусы
│   │   ├── Store.ts           # выдача покупок, в том числе незавершённых
│   │   └── Onboarding.ts      # обучение первых десяти минут
│   ├── content/               # ДАННЫЕ, НЕ КОД
│   │   ├── catches.json       # 40 видов рыбы и 12 предметов мусора
│   │   ├── zones.json         # локации: палитра, декор, пул, гейт, особенность
│   │   ├── bosses.json        # боссы: фазы боя, трофей, награда
│   │   ├── upgrades.json      # четыре ветки × пять уровней
│   │   ├── quests.json        # цепочка заданий
│   │   ├── dailies.json       # пул ежедневных дел
│   │   ├── products.json      # внутриигровые покупки
│   │   └── types.ts, catalog.ts, products.ts   # типы и доступ к данным
│   ├── scene3d/               # рендер: сцена от первого лица
│   │   ├── FishingScene3D.ts  # состояния заброса, склейка сцены
│   │   ├── Water3D.ts         # шейдер воды: зыбь, рябь, отражение, дымка
│   │   ├── Sky3D.ts           # купол, солнце, облака
│   │   ├── Environment3D.ts   # берег и наборы декора локаций
│   │   ├── Pier3D.ts          # помост под ногами
│   │   ├── Angler3D.ts        # сосед по причалу
│   │   ├── Birds3D.ts         # чайки
│   │   ├── FishView3D.ts      # модель улова: тело, плавники, изгиб
│   │   ├── AmbientFish3D.ts   # стая под водой и наведение крючка
│   │   ├── Hook3D.ts, Line3D.ts, Hands3D.ts    # крючок, леска, удилище
│   │   └── Splash3D.ts, Wake3D.ts              # всплеск и круги на воде
│   ├── platform/
│   │   ├── IPlatform.ts       # граница: реклама, сейв, лидерборды, покупки, язык
│   │   ├── YandexPlatform.ts  # реализация на SDK площадки
│   │   ├── LocalPlatform.ts   # заглушка для локальной разработки
│   │   └── yandex-sdk.d.ts    # типы SDK: официальных нет
│   ├── services/
│   │   ├── SaveService.ts     # локальный кэш плюс облако, миграции, проверка
│   │   ├── AdManager.ts       # кулдауны, пауза геймплея, баннер, «без рекламы»
│   │   ├── AudioService.ts    # синтез звука, прибой, пауза по потере фокуса
│   │   ├── Leaderboards.ts    # очередь результатов: у площадки лимит
│   │   └── I18n.ts            # ru/en, автоопределение языка
│   ├── ui/                    # DOM-оверлей поверх канваса
│   │   ├── GameUi.ts          # панели, шкалы, подсказки, навигация пультом
│   │   ├── CatchIcon.ts       # значок улова в альбоме
│   │   └── ArchipelagoMap.ts  # карта локаций
│   └── debug/PerfHud.ts       # кадр, вызовы отрисовки, треугольники
├── tools/
│   ├── build-zip.ts           # архив и проверки требований площадки
│   ├── capture.ts             # прогон всей игры в браузере: кадры и проверки
│   ├── shot.ts                # один кадр в файл
│   ├── balance.ts             # отчёт по балансу боёв
│   ├── tune-fights.ts         # подгонка длины боя и терпения по данным
│   └── promo.ts, promo/art.ts # иконка и обложка вектором
└── vite.config.ts
```

Чего в дереве нет намеренно:

- **менеджера сцен и шины событий** — сцена одна, а связи между системами явные: их видно в
  `App.ts` и можно прочитать целиком;
- **папки `assets/`** — в игре нет ни одной картинки и ни одного звукового файла: всё считается
  кодом, поэтому архив весит 170 КБ вместо десятков мегабайт;
- **аналитики** — у площадки нет своего API событий, а тянуть стороннюю систему в игру, где
  запрещены сторонние SDK, нельзя.

Ключевой принцип: **весь баланс — в `content/*.json`**, ни одного числа в коде игры. Правки
баланса не требуют участия программиста, а длину боя и терпение улова пересчитывает
`npm run tune-fights` по симулятору.

## 4.3 Слой платформы

Весь код Яндекс SDK живёт только в `platform/YandexPlatform.ts`. Игровая логика знает лишь
интерфейс `IPlatform`. Это даёт локальную разработку без портала и делает будущий порт (VK Play,
CrazyGames, Playdeck) вопросом одного файла.

```ts
export interface IPlatform {
  init(): Promise<void>;
  ready(): void;                     // LoadingAPI.ready()
  gameplayStart(): void;             // GameplayAPI.start()
  gameplayStop(): void;              // GameplayAPI.stop()
  lang(): string;
  isTV(): boolean;
  showRewarded(placement: string): Promise<boolean>;
  showInterstitial(): Promise<void>;
  showBanner(): Promise<void>;
  hideBanner(): Promise<void>;
  load(): Promise<SaveData | null>;
  save(data: SaveData): Promise<void>;
  submitScore(board: string, value: number): Promise<void>;
  canReview(): Promise<boolean>;     // feedback.canReview()
  requestReview(): Promise<boolean>; // feedback.requestReview()
  products(): Promise<Product[]>;    // payments.getCatalog()
  pendingPurchases(): Promise<Purchase[]>; // payments.getPurchases()
  purchase(productId: string): Promise<Purchase | null>;
  consumePurchase(token: string): Promise<void>;
}
```

### Подключение SDK

Скрипт SDK подключается в `index.html` **до** вызова `YaGames.init()`:

```html
<script src="/sdk.js"></script>
```

```ts
// YandexPlatform.ts
async init() {
  this.ysdk = await YaGames.init();
  this.player = await this.ysdk.getPlayer();      // гостевой режим допустим
  this.locale = this.ysdk.environment.i18n.lang;  // автоопределение языка
}

ready() { this.ysdk.features.LoadingAPI?.ready(); }
gameplayStart() { this.ysdk.features.GameplayAPI?.start(); }
gameplayStop() { this.ysdk.features.GameplayAPI?.stop(); }
```

> Авторизацию через `ysdk.auth.openAuthDialog()` не вызываем вовсе: сессия короткая, прогресс
> живёт локально и уходит в облако, если игрок уже авторизован. Требование платформы про осознанное
> действие соблюдено тем, что окна авторизации нет. Если понадобится синхронизация между
> устройствами — только по кнопке, а не при запуске.

### Реклама

```ts
async showInterstitial() {
  this.game.pause();
  this.audio.muteAll();
  this.gameplayStop();
  await new Promise<void>(resolve => {
    this.ysdk.adv.showFullscreenAdv({
      callbacks: { onClose: () => resolve(), onError: () => resolve() },
    });
  });
  this.audio.unmuteAll();
  this.game.resume();
  this.gameplayStart();
}
```

Правила `AdManager`:
- кулдаун полноэкранной рекламы — не менее 180 секунд и только вне активного геймплея;
- rewarded — без кулдауна, но каждая точка показывает награду **до** запуска ролика;
- награда выдаётся строго в `onRewarded`, а не в `onClose`;
- если `wasShown === false` — награду не выдаём, но и не наказываем игрока.

### Сохранения

```ts
// SaveService: локальный кэш — источник правды в рантайме, облако — синхронизация
save(data: SaveData) {
  localStorage.setItem(KEY, JSON.stringify(data));   // мгновенно
  this.scheduleCloudFlush();                          // дебаунс 10 сек
}
```

- Лимиты SDK: `setData` — 100 запросов / 5 минут, ≤ 200 КБ. Дебаунс 10 секунд с гарантированным
  флашем перед показом рекламы, на `visibilitychange` и `pagehide`.
- Формат сейва версионируется (`{version: 3, ...}`), миграции — чистые функции `migrate_2_to_3`.
- Числовые агрегаты (крупнейшая рыба, всего поймано) дублируем в `setStats` — дешевле и быстрее.
- При старте: локальный сейв ↔ облачный, побеждает больший `updatedAt`; конфликт разрешаем в
  пользу игрока (максимум по каждому счётчику).

### Аудио и фокус

Требование платформы: звук останавливается при потере фокуса. Реализуем один раз в `AudioService`:

```ts
document.addEventListener('visibilitychange', () => {
  document.hidden ? this.suspend() : this.resume();
});
window.addEventListener('blur', () => this.suspend());
window.addEventListener('focus', () => this.resume());
```

Плюс `GameplayAPI.stop()` при уходе и `start()` при возврате. Форматы: `.ogg` + `.m4a` (Safari),
общий бюджет звука — 3 МБ.

## 4.4 Бюджеты производительности

| Показатель | Бюджет | Факт |
|---|---|---|
| Вес архива (распакованный) | ≤ 25 МБ (лимит платформы 100 МБ) | **0.65 МБ** |
| Время до первого экрана | ≤ 3 сек на 4G | один файл 170 КБ в архиве |
| Время до `LoadingAPI.ready()` | ≤ 6 сек | вызывается на первом готовом кадре |
| FPS на бюджетном телефоне | ≥ 30 стабильно, 60 на среднем сегменте | проверяется только на устройстве |
| Вызовов отрисовки на кадр | ≤ 120 | **89** на бюджетном профиле |
| Треугольников на кадр | ≤ 40 000 | **18 140** на бюджетном профиле |
| Пиковая память | ≤ 250 МБ | текстур нет вовсе |

Ленивая догрузка локаций не понадобилась: весь контент — данные и процедурная геометрия, поэтому
архив на два порядка меньше бюджета. Вызовы отрисовки и треугольники показывает HUD (`H`), сетка
поверхности воды и тени зависят от профиля качества (`?q=low|high`).

## 4.5 Адаптивность и ввод

- **Портрет (мобильные, приоритет):** вид от первого лица с причала, вся управляющая зона — нижняя
  треть экрана, игра проходится **одним большим пальцем**. Проверено на 360 CSS-пикселях с
  шестизначным кошельком: нижний ряд не переносится и не уезжает за край.
- **Ландшафт (десктоп):** та же сцена, шире по горизонтали; соотношение активного поля держим в
  пределах 2:1 — на сверхшироком мониторе поле ограничивается и центрируется вместе с интерфейсом.
  Управление мышью, пробел/стрелки как альтернатива.
- **TV:** навигация стрелками пульта, обработка OK и Back, фокус-рамка на всех интерактивных
  элементах, магазин скрыт (покупки на TV запрещены).
- Отключаем контекстное меню в игровой области (`contextmenu` → `preventDefault`) — частая
  причина отказа модерации.
- Никакого системного скроллбара: `html, body { overflow: hidden; overscroll-behavior: none; }`.

## 4.6 Инструменты и процесс

- **Сборка:** Vite → `dist/` → `tools/build-zip.ts` пакует ZIP и **проваливает сборку**, если
  распакованный размер > 100 МБ, есть кириллица/пробелы в именах или отсутствует `index.html`
  в корне.
- **Локальная разработка:** `LocalPlatform` подменяет SDK (реклама — заглушка на 2 секунды,
  сейвы — localStorage), поэтому портал не нужен до самого тестирования черновика.
- **Проверки:** `npm run build` — типы, 111 unit-тестов и сборка; `npm run capture` — прогон всей
  игры в реальном браузере, который падает на любой ошибке в консоли и проверяет цикл, покупки,
  обучение, пульт, локализацию, узкий и сверхширокий экран, потерю контекста и отсутствие WebGL;
  `npm run balance` — отчёт по балансу боёв пятью моделями игрока.
- **Тестирование на площадке:** черновик в консоли работает с реальной рекламой, авторизацией и
  облачными сохранениями — обязательный этап перед отправкой.
- **Аналитика:** только метрики консоли разработчика. Своей аналитики в игре нет: у площадки нет
  API событий, а сторонние SDK в играх запрещены.
